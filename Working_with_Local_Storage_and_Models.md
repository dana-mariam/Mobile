30 jul 
tharesday

---





# Practical Application

اليوم كان مختلف عن الأيام السابقة، لأنه لأول مرة صار التطبيق يتعامل مع أكثر من Task بدل Task واحدة فقط.

بالبداية تعلمت كيف أخزن البيانات المعقدة داخل `SharedPreferences` باستخدام `jsonEncode` و `jsonDecode`، لأنه اكتشفت إنه `SharedPreferences` ما بقدر يخزن `Map` أو `List` بشكل مباشر، ولازم تتحول البيانات إلى String أولًا.

بعدها طبقت حفظ Task كاملة تحتوي على:

- Task Name
- Task Description
- High Priority

وبعد حفظها داخل `SharedPreferences` رجعتها مرة ثانية للتطبيق عن طريق `jsonDecode`.

بعدها انتقلنا لتخزين أكثر من Task داخل List بدل ما يكون التطبيق يتعامل مع Task واحدة فقط، وصار كل Task جديدة تنضاف إلى القائمة بدل ما تستبدل القديمة.

بعدها تعلمت كيف أستخدم `ListView` حتى أعرض جميع التاسكات الموجودة داخل القائمة بشكل مرتب، بحيث كل Task تظهر تحت الثانية بشكل تلقائي.

كمان تعلمت استخدام `CheckBox` وربطها مع كل Task، وبعدها انتقلنا لاستخدام `Model` حتى يصير الكود مرتب وأسهل بالتعديل بدل الاعتماد على `Map<String, dynamic>` بكل مكان.

---

# Key Concepts Learned

## JsonEncode

تعلمت إن `SharedPreferences` ما بقدر يخزن Object أو Map بشكل مباشر.

لذلك أول خطوة لازم أحول البيانات إلى String باستخدام:

```dart
jsonEncode(task)
```

بعدها بقدر أخزنها داخل SharedPreferences.

---

## JsonDecode

لما أرجع أقرأ البيانات من SharedPreferences.

بتكون عبارة عن String.

حتى أرجعها لنفس شكلها الأصلي لازم أستخدم:

```dart
jsonDecode(taskJson)
```

وبعدها أقدر أوصل لكل قيمة داخلها.

---

## SharedPreferences With List

بالبداية التطبيق كان يخزن Task واحدة فقط.

وكل مرة أضيف Task جديدة كانت تمسح القديمة.

الحل كان:

- قراءة القائمة القديمة.
- تحويلها باستخدام jsonDecode.
- إضافة التاسك الجديدة داخل القائمة.
- إعادة تحويل القائمة إلى String.
- حفظها مرة ثانية داخل SharedPreferences.

هيك صار التطبيق يحتفظ بكل التاسكات.

---

## Work With Models

قبل اليوم كنا نتعامل مع البيانات باستخدام:

```dart
Map<String,dynamic>
```

لكن مع زيادة البيانات صار الكود أصعب بالقراءة.

لذلك أنشأنا Model خاص بالتاسك.

صار يحتوي على:

- taskName
- taskDescription
- isHighPriority

وبهيك صار الكود أوضح وأسهل للصيانة.

---

## toMap()

تعلمت إن `toMap()` بتحول الـ Object إلى Map.

وهذا بيسهل عملية تحويله بعدين إلى JSON حتى ينحفظ داخل SharedPreferences.

---

## fromJson()

بعد قراءة البيانات باستخدام `jsonDecode`.

بتكون البيانات Map.

لذلك استخدمنا `fromJson()` حتى نحولها مرة ثانية إلى TaskModel.

وهيك رجعت البيانات Object أقدر أتعامل معها داخل التطبيق بسهولة.
---

## ListView

بالبداية كنا نعرض كل Task بشكل يدوي، ولو صار عندنا أكثر من Task كنا رح نضطر نكرر نفس الكود كل مرة، وهذا إشي غير عملي.

بعدها تعلمت استخدام `ListView`، وهو Widget مخصص لعرض عدد كبير من العناصر داخل قائمة قابلة للتمرير (Scrollable).

حتى يعرض جميع التاسكات استخدمنا `for` loop داخل `children`، وصار كل عنصر داخل القائمة ينرسم تلقائيًا حسب عدد التاسكات الموجودة.

وبدل ما أكرر الكود، صار التطبيق يبني العناصر بشكل ديناميكي.

---

## For Loop

استخدمنا `for` loop حتى نعرض جميع التاسكات الموجودة داخل القائمة.

بدل ما أكتب Widget لكل Task بشكل يدوي.

صار Flutter يلف على جميع عناصر الـ List، ولكل عنصر يبني Widget جديدة.

وهذا وفر علينا كتابة كود كثير، وصار التطبيق يدعم أي عدد من التاسكات.

---

## CheckBox

بالبداية كنت مفكرة إنه `CheckBox` نفس فكرة `Switch`، لكن بعد التطبيق فهمت إنه وظيفته تحديد قيمة منطقية (`true` أو `false`) لكل عنصر.

طبقناه حتى نحدد إذا المهمة منتهية أو لا.

وربطنا قيمة الـ CheckBox مع بيانات كل Task.

---

## Shape

تعلمت إنه أقدر أغير شكل الـ CheckBox باستخدام خاصية `shape`.

وصار بإمكاني أتحكم إذا بدي شكله دائري أو بزوايا دائرية بدل الشكل الافتراضي.

وهذا بيعطي شكل أجمل للواجهة.

---

## Updating Data in SharedPreferences

بعد ما غيرنا قيمة الـ CheckBox.

اكتشفنا إنه لو سكرت التطبيق ورجعت فتحته، رح ترجع القيمة القديمة إذا ما خزناها مرة ثانية.

لذلك بعد كل تعديل على أي Task.

صرنا نعمل:

- تعديل البيانات داخل القائمة.
- تحويل القائمة باستخدام `jsonEncode`.
- حفظها مرة ثانية داخل `SharedPreferences`.

وبهيك ظل أي تعديل محفوظ حتى بعد إغلاق التطبيق.

---

# Problems Faced

### المشكلة الأولى

بعد ما استخدمت `jsonDecode`.

طلع نوع البيانات:

```dart
dynamic
```

لكن أنا كنت بدي أتعامل معها كـ

```dart
Map<String,dynamic>
```

لذلك احتجت أستخدم Casting حتى أحدد نوع البيانات الصحيح.

---

### المشكلة الثانية

بعد ما انتقلنا من Task واحدة إلى List من التاسكات.

ظهر الخطأ:

```dart
type 'List<dynamic>' is not a subtype of type 'Map<String,dynamic>'
```

سبب المشكلة:

لأني كنت أتعامل مع البيانات كأنها `Map` بينما هي صارت `List`.

الحل:

عدلت نوع البيانات وصرت أتعامل معها كقائمة، وبعدها ألف على جميع العناصر باستخدام Loop.

---

### المشكلة الثالثة

لما طبقت CheckBox.

أي CheckBox أكبس عليها كانت كل التاسكات تتغير بنفس الوقت.

سبب المشكلة:

لأني كنت مستخدمة متغير واحد فقط لكل القائمة.

فكل العناصر كانت تشترك بنفس القيمة.

الحل:

أضفنا متغير

```dart
isHighPriority
```

داخل `TaskModel`.

وصار لكل Task نسخة خاصة فيها، لذلك صار كل CheckBox يشتغل بشكل مستقل.

---

### المشكلة الرابعة

بالبداية ما كان عندي Constructor داخل الـ Model.

فما كنت أقدر أنشئ Object جديد بسهولة.

بعدها أضفنا Constructor يستقبل جميع القيم.

وبالتالي صار إنشاء Task جديدة أسهل وأكثر ترتيبًا.

---

### المشكلة الخامسة

بعد ما صار عندي عدد كبير من التاسكات.

كل النصوص صارت تطلع خارج حدود الـ Container.

الحل كان استخدام:

```dart
maxLines
```

حتى أحدد عدد الأسطر المسموح بها.

واستخدمنا:

```dart
overflow
```

حتى لو كان النص طويل ينقصه بشكل مرتب بدل ما يخرب شكل الواجهة.

---

# Results

بنهاية اليوم صار التطبيق يقدر يخزن أكثر من Task داخل `SharedPreferences`، ويعرضها باستخدام `ListView` بشكل مرتب.

وصرت أعرف أحول البيانات بين Object وJSON باستخدام `jsonEncode` و`jsonDecode`.

كمان فهمت أهمية استخدام Models بدل التعامل مع `Map` مباشرة، لأنها بتخلي الكود أوضح وأسهل للتعديل.

وأخيرًا تعلمت كيف أحدث البيانات داخل `SharedPreferences` بحيث أي تعديل على التاسكات يضل محفوظ حتى بعد إغلاق التطبيق.

---

# Questions & Answers

## 1. ليش استخدمنا jsonEncode؟

لأن `SharedPreferences` بخزن أنواع بيانات بسيطة فقط، لذلك لازم نحول الـ Object أو الـ List إلى String قبل التخزين.

---

## 2. ليش استخدمنا Model بدل Map؟

لأن الـ Model بخلي الكود أوضح، وأسهل بالقراءة والصيانة، وبقلل الأخطاء لما يكبر المشروع.

---

## 3. ليش استخدمنا ListView؟

لأنه بعرض عدد كبير من العناصر بشكل مرتب، وبيعطي إمكانية التمرير بدون الحاجة لكتابة Widget لكل عنصر.

---

## 4. ليش كانت كل الـ CheckBox تتغير مع بعض؟

لأنها كانت مرتبطة بنفس المتغير. بعد ما صار لكل Task متغير خاص داخل الـ Model، صار كل CheckBox يشتغل بشكل مستقل.

---

## 5. ليش استخدمنا maxLines وoverflow؟

حتى نحافظ على شكل الواجهة إذا كان النص طويل، وما يطلع خارج حدود الـ Container أو يخرب ترتيب الشاشة.

---

# Reflection

اليوم حسيت إني بدأت أتعامل مع التطبيق بطريقة أقرب للمشاريع الحقيقية. أكثر جزء احتاج مني تركيز كان `jsonEncode` و`jsonDecode`، لأنه بالبداية ما كنت فاهمة ليش لازم نحول البيانات قبل التخزين وبعدها نرجعها لشكلها الأصلي. كمان استخدام الـ Models خلاني أشوف كيف الكود بصير أوضح وأسهل مع زيادة حجم المشروع. واجهت أكثر من Error أثناء التطبيق، لكن لما فهمت سبب كل واحد وحليته، حسيت إني استفدت أكثر من مجرد مشاهدة الفيديو.
# Code Explanation

بالبداية كنا نخزن Task واحدة داخل `SharedPreferences`، لكن بعد ما صرنا نشتغل على أكثر من Task احتجنا نغير طريقة التخزين.

أول خطوة كانت قراءة البيانات المحفوظة:

```dart
final taskJson = pref.getString("tasks");
```

إذا كانت البيانات موجودة، كنا نحولها من String إلى List باستخدام:

```dart
jsonDecode(taskJson);
```

بعدها نضيف التاسك الجديدة إلى القائمة:

```dart
listTasks.add(task);
```

ثم نحول القائمة كاملة إلى String باستخدام:

```dart
jsonEncode(listTasks);
```

وأخيرًا نحفظها مرة ثانية داخل `SharedPreferences` حتى تظل جميع التعديلات محفوظة.

بعدها أنشأنا `TaskModel`، وصارت كل البيانات الخاصة بالمهمة موجودة داخل Object واحد بدل استخدام `Map` في كل مكان.

ولما كنا نقرأ البيانات، استخدمنا `fromJson()` حتى نحول كل عنصر داخل القائمة إلى `TaskModel`، وبهيك صار التعامل مع البيانات أسهل وأكثر ترتيبًا.

---

# Extra Concepts Learned

## Strong Typing

اليوم فهمت أكثر ليش الأفضل أحدد نوع البيانات بدل استخدام `dynamic` بكل مكان.

لما يكون لكل متغير نوع واضح، الكود بصير أسهل بالقراءة، والأخطاء بتظهر أثناء البرمجة بدل ما تظهر وقت تشغيل التطبيق.

---

## Constructor

تعلمت إنه الـ Constructor بيسهل إنشاء Object جديد.

بدل ما أضيف القيم واحدة واحدة، بقدر أمررها كلها مرة واحدة عند إنشاء الـ Model.

وهذا بخلي الكود أنظف وأسهل.

---

## Key داخل Map

تعلمت إنه كل قيمة داخل الـ Map إلها Key خاص فيها.

مثل:

```dart
task["taskName"]
task["taskDescription"]
task["isHighPriority"]
```

ومن خلال الـ Key بقدر أوصل للقيمة المطلوبة.

---

## الفرق بين jsonEncode و jsonDecode

`jsonEncode`

يحول Object أو List إلى String حتى أقدر أخزنها.

أما

`jsonDecode`

فيرجع الـ String إلى List أو Map حتى أستخدم البيانات داخل التطبيق.

---

# Challenges

أكثر إشي احتاج مني تركيز هو الانتقال من استخدام Task واحدة إلى List من التاسكات.

بالبداية كنت أتعامل مع البيانات بنفس الطريقة القديمة، لذلك ظهرت أخطاء كثيرة بسبب اختلاف نوع البيانات.

كمان احتجت وقت حتى أفهم ليش أنشأنا Model، وليش ما ضلينا نستخدم Map فقط.

بعد التطبيق أكثر من مرة، صار الفرق أوضح بالنسبة إلي.

---

# What I Learned

بنهاية اليوم صرت أقدر:

- أخزن أكثر من Task داخل SharedPreferences.
- أستخدم jsonEncode و jsonDecode بشكل صحيح.
- أتعامل مع List من البيانات بدل عنصر واحد.
- أبني Model خاص بالبيانات.
- أحول البيانات باستخدام toMap و fromJson.
- أعرض البيانات باستخدام ListView.
- أستخدم For Loop لبناء Widgets بشكل ديناميكي.
- أتعامل مع CheckBox لكل Task بشكل مستقل.
- أحدث البيانات داخل SharedPreferences بعد أي تعديل.
- أحل مشاكل Overflow باستخدام maxLines و overflow.

---

# Overall Reflection

اليوم كان من أكثر الأيام اللي حسيت فيها إنه التطبيق صار أقرب لمشروع حقيقي. بالبداية كانت المواضيع كثيرة ومترابطة، خصوصًا لما انتقلنا من تخزين Task واحدة إلى إدارة قائمة كاملة من التاسكات، لكن مع التطبيق خطوة بخطوة بلشت أفهم كيف كل جزء مرتبط بالثاني. أكثر شيء استفدت منه هو فهم فكرة الـ Models، لأنها خلت الكود أوضح وأسهل من استخدام Map في كل مكان. صحيح واجهت أكثر من Error أثناء التطبيق، لكن حلها خلاني أفهم سبب المشكلة وكيف أتجنبها بالمرة الجاية.
---

## IconButton

تعلمت استخدام `IconButton` لإضافة زر عبارة عن أيقونة فقط بدل زر يحتوي على نص.

استخدمناه لتنفيذ أوامر مثل الحذف أو التعديل أو أي عملية تحتاج ضغطة سريعة، وميزته إنه بسيط وما بياخد مساحة كبيرة داخل الواجهة.

---

## GestureDetector

تعلمت استخدام `GestureDetector` حتى أخلي أي Widget يستجيب للمس أو الضغط، حتى لو ما كان Button.

قدرنا من خلاله ننفذ أوامر عند الضغط على العنصر، وهذا أعطانا حرية أكبر بالتعامل مع الواجهة.

---

## Expanded

تعلمت استخدام `Expanded` لما يكون عندي أكثر من Widget داخل `Row` أو `Column`.

وظيفته إنه يخلي الـ Widget يأخذ المساحة المتبقية بشكل مناسب، وبهيك ما يصير في مشاكل بالأحجام أو يطلع جزء من العناصر خارج الشاشة.

---

## Text Overflow

بعد ما صار عندي أكثر من Task، وبعض أسماء التاسكات كانت طويلة، لاحظت إن النص صار يطلع خارج حدود الـ Container ويخرب شكل الواجهة.

حلينا المشكلة باستخدام:

```dart
maxLines
```

حتى نحدد عدد الأسطر المسموح فيها.

واستخدمنا:

```dart
overflow: TextOverflow.ellipsis
```

حتى إذا كان النص أطول من المساحة، يظهر بشكل مرتب مع (...) بدل ما يطلع خارج حدود العنصر.

---

# Problems Faced

### المشكلة السادسة

بعد إضافة عدد كبير من التاسكات، كل عنصر صار يأخذ مساحة أكبر من اللازم، وبعض النصوص خرجت خارج الـ Container.

**السبب:**

ما كان في تحديد لعدد الأسطر أو طريقة عرض النص الطويل.

**الحل:**

استخدمنا:

- `maxLines`
- `TextOverflow.ellipsis`

حتى نحافظ على شكل الواجهة.

---

### المشكلة السابعة

بالبداية كل عناصر الصفحة كانت تظهر فوق بعض، وما كان في مساحة كافية لعرضها بشكل مرتب.

**السبب:**

ارتفاع الـ Widget ما كان مناسب للمحتوى.

**الحل:**

عدلت قيمة الـ `height` للـ Container، وبعدها صار كل عنصر يظهر بمكانه الصحيح.

---

### المشكلة الثامنة

بعد ما عدلت قيمة الـ CheckBox، كان التعديل يختفي أول ما أسكر التطبيق.

**السبب:**

كنت أعدل القيمة داخل التطبيق فقط بدون ما أخزنها مرة ثانية.

**الحل:**

بعد كل تعديل صرت:

- أعدل القيمة داخل الـ List.
- أحول البيانات باستخدام `jsonEncode`.
- أخزنها مرة ثانية داخل `SharedPreferences`.

وبهيك صار أي تعديل يظل محفوظ حتى بعد إغلاق التطبيق.

---

# Additional Questions & Answers

## 6. ليش استخدمنا Model بدل Map؟

لأن الـ Model بنظم البيانات داخل Class واحد، وبيخلي الكود أوضح وأسهل للصيانة، خاصة لما يكبر المشروع.

---

## 7. ليش استخدمنا jsonEncode قبل التخزين؟

لأن `SharedPreferences` ما بخزن Objects أو Lists مباشرة، وإنما بخزن String، لذلك لازم نحول البيانات أولًا.

---

## 8. ليش استخدمنا jsonDecode بعد القراءة؟

حتى نرجع البيانات من String إلى List أو Map ونقدر نتعامل معها داخل التطبيق.

---

## 9. ليش استخدمنا ListView بدل Column؟

لأن `Column` بعرض العناصر كلها مرة واحدة، وإذا صار عددها كبير رح يطلع Overflow.

أما `ListView` فهو قابل للتمرير، وبقدر يعرض أي عدد من العناصر.

---

## 10. ليش استخدمنا Expanded؟

حتى يوزع المساحة بين العناصر بشكل صحيح، ويمنع ظهور مشاكل بالأحجام داخل `Row` أو `Column`.
